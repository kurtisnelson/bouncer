table.table
  thead
    tr
      th Name
      th Phone
      th Email
  tbody
    - @users.each do |user|
      tr
        td = user.name
        td = user.phone
        td = user.email
        td = link_to "Show", user_path(user)
